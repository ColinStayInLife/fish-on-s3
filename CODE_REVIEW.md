# 🎣 FishOn-S3 代码审查与改进建议

> 审查日期: 2026-04-28 | 审查者: AI Code Review
> 项目阶段: 概念设计 → 即将进入 Phase 1 开发

---

## 目录

1. [文档审查](#1-文档审查)
2. [项目结构](#2-项目结构)
3. [架构设计](#3-架构设计)
4. [BLE 通信协议](#4-ble-通信协议)
5. [IMU 鱼口检测算法](#5-imu-鱼口检测算法)
6. [功耗与续航](#6-功耗与续航)
7. [技术风险](#7-技术风险)
8. [Firmware 代码结构建议](#8-firmware-代码结构建议)
9. [Phase 1 & 2 骨架代码](#9-phase-1--2-骨架代码)

---

## 1. 文档审查

### 1.1 一致性检查

| 文件 | 主机型号 | 子机成本 | 问题 |
|:----|:--------|:--------|:-----|
| `README.md` | ✅ M5StickC Plus | ✅ ¥12/竿 | 一致 |
| `docs/product-definition.md` | ✅ M5StickC Plus | ✅ ¥12/竿 | **V1.1 已更新** |
| `hardware/rod-sensor-design.md` | — | ✅ ¥12/竿 | 一致 |
| `research/market-research.md` | ❌ **CoreS3 SE** | ❌ **¥5-8/竿** | ❌ **未更新** |

### 1.2 关键问题

**🔴 严重：`research/market-research.md` 仍引用已废弃的 CoreS3 SE 方案**
- 第 3 行: "项目：基于 M5Stack CoreS3 SE 的智能鱼口检测系统"
- 第 86 行: "CoreS3 SE 钓鱼相关项目（非报警器）"
- 第 92 行: 4.1 节全部分析 CoreS3 SE 硬件
- 第 148 行: 图表的 "CoreS3 SE 已有" 与文字 "M5StickC Plus" 矛盾
- 第 167 行: "BLE多竿监控（一个CoreS3监控所有竿）"
- 第 177 行: 成本表仍写 "CoreS3 SE 已有"
- 第 191 行: "成本: CoreS3 SE 已有，额外每竿¥5-8"
- 第 198 行: "CoreS3 SE 读取 IMU 数据"

**🟡 中：`research/market-research.md` 与实际文档成本不一致**
- market-research 写 **¥5-8/发射器**，但硬件设计写 **¥12**
- 差异约 ¥4-7，虽然不大但说明文档未同步

**🟡 中：`README.md` 开发路线缺少 Phase 4-6 的交付物定义**
- Phase 1-3 有交付物说明，Phase 4-6 只有标题

**🟢 轻微：产品定义中的"步进实现"可再细化**
- 状态机设计缺少"自动复位到待命"的超时机制定义

### 1.3 缺失文档

| 缺失文档 | 必要性 | 建议 |
|:---------|:------|:-----|
| `docs/ble-protocol.md` | 🔴 必要 | 独立的 BLE 协议规格说明书 |
| `docs/testing-plan.md` | 🟡 重要 | 测试用例，尤其是河边实测方案 |
| `docs/single-rod-mode.md` | 🟡 重要 | 单竿模式下 StickC 自己的 IMU 使用逻辑 |
| `CONTRIBUTING.md` | 🟢 可选 | 如果开源 |

---

## 2. 项目结构

### 2.1 当前结构

```
fish-on-s3/
├── README.md                     # 项目总览
├── docs/
│   └── product-definition.md     # 产品定义说明书
├── hardware/
│   └── rod-sensor-design.md      # 竿梢子机设计方案
├── research/
│   └── market-research.md        # 市场调研报告
├── firmware/                     # ⚠️ 空目录
└── .gitignore
```

### 2.2 建议结构

```
fish-on-s3/
├── README.md
├── CODE_REVIEW.md                # ← 本文件
├── docs/
│   ├── product-definition.md
│   ├── ble-protocol.md            # ← BLE 通信协议规格
│   ├── algorithm.md               # ← IMU 鱼口检测算法
│   └── testing-plan.md            # ← 测试方案
├── hardware/
│   ├── rod-sensor-design.md
│   ├── pcb/                       # ← 如果将来打样PCB
│   │   ├── rod-sensor/            #   子机PCB设计
│   │   └── enclosure/             #   3D打印外壳
│   └── schematics/
│       └── rod-sensor-wiring.png  # ← 接线图（图片版）
├── firmware/
│   ├── host-stickc/               # ← M5StickC Plus 主机固件
│   │   ├── platformio.ini
│   │   └── src/
│   │       ├── main.cpp
│   │       ├── BLEScanner.h/.
│   │       ├── DisplayManager.h/.
│   │       ├── AudioAlert.h/.
│   │       ├── StateManager.h/.
│   │       └── LogManager.h/.
│   └── node-esp32c3/             # ← 竿梢子机固件
│       ├── platformio.ini
│       └── src/
│           ├── main.cpp
│           ├── IMUReader.h/.
│           ├── VibrationAnalyzer.h/.
│           ├── BleBroadcaster.h/.
│           ├── PowerManager.h/.
│           └── RodConfig.h
├── shared/
│   └── BleProtocol.h             # ← BLE 协议定义（两边共用）
├── research/
│   ├── market-research.md
│   ├── log-archive/              # ← 实钓测试数据归档
│   └── imu-data-samples/         # ← 各类鱼口波形样本
├── tools/
│   ├── imu-logger.py             # ← USB串口 IMU 数据记录
│   └── analyze-waveform.py       # ← 离线波形分析
├── .gitignore
└── LICENSE
```

### 2.3 结构改进要点

1. **`shared/` 目录**：BLE 协议头文件两边共用，确保定义一致
2. **`firmware/` 分两个子项目**：host-stickc 和 node-esp32c3，各自独立 platformio.ini
3. **`tools/` 目录**：PC端调试工具，辅助算法调参
4. **`docs/` 补充**：ble-protocol.md, algorithm.md, testing-plan.md

---

## 3. 架构设计审查

### 3.1 架构图评估

```
竿梢子机 (ESP32-C3 + MPU6050)     ← OK
  │
  │ ① 100Hz 加速度采样            ← OK
  │ ② 本地振动分析                 ← ✅ 正确的设计决策
  │ ③ BLE 广播(4字节)             ← OK
  │
BLE 无线连接
  │
  ▼
M5StickC Plus (主机)              ← OK
  │ ④ BLE 扫描                    ← OK
  │ ⑤ 显示 + 蜂鸣                 ← OK
```

**设计评价**：边端计算的设计策略是正确的，BLE作为瘦传输层。

### 3.2 🔴 架构隐患

**隐患 1：BLE 广播风暴**
- **问题**：当 8 个子机同时检测到鱼口，每个子机以 100ms 间隔广播 → 空中每秒 80 个 BLE 包。虽然 BLE 38 个信道理论上承受得住，但主机扫描周期(典型 100-300ms/次)可能漏包。
- **风险等级**：🟡 中
- **缓解**：① 子机广播使用随机延迟 (0-50ms jitter) ② 主机扫描窗口开到最大 (~300ms) ③ 使用广播间隔自适应（无鱼5s广播一次，有鱼200ms一次）

**隐患 2：CR2032 驱动 ESP32-C3 的电压问题**
- **问题**：CR2032 标称 3V，新电池 3.2V，大部分 C3 板内置 LDO 需要 Vin > 3.3V。CR2032 放电曲线在 2.8V 以下时 ESP32-C3 可能不稳定或无法启动。
- **风险等级**：🔴 严重
- **缓解**：① 确认所用 C3 板的 LDO 型号和 dropout 电压 ② 考虑使用 2×AAA（3V）或 14500 锂电池（3.7V→LDO→3.3V） ③ 或在 CR2032 和 VIN 之间加升压小板

**隐患 3：未定义设备发现和配对的流程**
- **问题**：8 个子机如何与主机配对？如果邻居也在用，如何避免串扰？
- **风险等级**：🟡 中
- **缓解**：① 使用白名单机制（主机只识别已知 MAC/ID） ② 子机广播数据包含固定前缀/"Fish"标识 ③ 通过按键 + 子机扫描配对模式

**隐患 4：子机没有 OTA 更新能力**
- **问题**：子机一旦烧录，更新固件需要拆热缩管 → USB 烧录 → 重新封装。
- **风险等级**：🟡 中
- **缓解**：① 考虑 ESP32-C3 的 OTA 通过 BLE 实现（复杂度较高） ② Phase 1 先用有线烧录，后续 Phase 6+ 加入 BLE OTA ③ 在硬件设计阶段就预留 OTA 可能需要的 flash 分区

**隐患 5：MPU6050 的 Z 轴安装方向歧义**
- **问题**：子机绑在竿上的方向不确定（竿水平 vs 倾斜），重力分量的投影不同，影响静止基准计算。
- **风险等级**：🟡 中
- **缓解**：① 上电自动校准静止基准（取前 100 个样本的均值重力矢量） ② 用「幅值相对于动态基准」而非「绝对幅值」

**隐患 6：单竿模式下主机功耗**
- **问题**：单竿模式直接夹竿上，StickC 的 MPU6886 100Hz 采样 + 屏幕常亮，950mAh 电池标称 2-3h 可能不够一轮夜钓。
- **风险等级**：🟡 中
- **缓解**：① 屏幕在无鱼口时 30s 后自动熄灭，触摸/振动唤醒 ② 采样率可降至 50Hz（竿梢振动通常 <50Hz） ③ 停用 BLE 扫描（单竿不需要）

---

## 4. BLE 通信协议

### 4.1 当前设计评估

```
广播包（4字节）：
┌────────┬────────┬────────┬────────┐
│ 竿ID   │ 状态码 │ 幅度   │ 电量   │
│ 1 byte │ 1 byte │ 1 byte │ 1 byte │
└────────┴────────┴────────┴────────┘
```

**优点**：
- ✅ 极简设计，广播时间短 (<0.5ms)
- ✅ 4字节可以塞进 manufacturer specific data
- ✅ 竿ID (0-7) 和状态码 (0-3) 设计合理

**弱点**：
- ⚠️ 无序列号/时间戳 → 无法检测丢包或时延
- ⚠️ 无 CRC → 数据损坏无法自检（BLE 链路层有 CRC，但 manufacturer data 内的 CRC 更好）
- ⚠️ 幅度用 1 byte (0-100) 精度太低（对应 0-±16g 量程时分辨率 0.32g/step，无法精确区分 0.3g 小鱼阈值）

### 4.2 建议改进版协议

```
广播包（6-8 字节，在 BLE 31 字节限制内）：

┌────────┬────────┬────────┬────────┬────────┬────────┬────────┬────────┐
│ Byte 0 │ Byte 1 │ Byte 2 │ Byte 3 │ Byte 4 │ Byte 5 │ Byte 6 │ Byte 7 │
├────────┼────────┼────────┼────────┼────────┼────────┼────────┼────────┤
│ 魔数   │ 竿ID   │ 状态码 │ 幅度H  │ 幅度L  │ 电量   │ 序列号 │ 计数   │
│ 0xA5   │ 0-15   │ 0-7    │ msb    │ lsb    │ 0-100  │ 0-255  │ 0-255  │
└────────┴────────┴────────┴────────┴────────┴────────┴────────┴────────┘
```

| 字段 | 说明 |
|:----|:-----|
| 魔数 0xA5 | 识别本协议标志，过滤其他 BLE 设备 |
| 竿ID 0-15 | 支持最多 16 个子机（8 竿余量） |
| 状态码 0-6 | 0=待命, 1=疑似, 2=中鱼, 3=爆口, 4=深睡, 5=错误, 6=心跳 |
| 幅度 16bit | 0-65535 对应 0-±16g，分辨率 ~0.5mg |
| 电量 0-100 | CR2032 电压映射百分比 |
| 序列号 | 每广播递增，主机可检测丢包 |
| 计数 | 累计检测到事件的次数 |

**如果不希望增加广播字节**，最少改进方案（6 字节）：

```
┌────────┬────────┬────────┬────────┬────────┬────────┐
│ 魔数   │ 竿ID   │ 状态码 │ 幅度H  │ 幅度L  │ 电量   │
│ 0xA5   │ 0-15   │ 0-7    │ msb    │ lsb    │ 0-100  │
└────────┴────────┴────────┴────────┴────────┴────────┘
```

### 4.3 BLE 通信时序

```
主机扫描窗口 (StickC)
├───────────────────────────────────┤
    ↓       ↓       ↓       ↓        (每 ~150ms 一个扫描窗口)
    ↑       ↑       ↑       ↑
    ├───────────────┤                 子机广播间隔 (5s 无鱼)
    ├─────┤                           子机广播间隔 (200ms 有鱼)

通信周期：
  子机深度睡眠 (~10μA)
    → MPU6050 运动检测中断唤醒
    → ESP32-C3 醒来采集 2s 数据
    → 分析结果 → BLE 广播 3 次（防丢包）
    → 回到浅睡 / 深睡
```

### 4.4 配对流程设计（缺失，建议补充）

```
Step 1: 子机上电 → 持续广播配对请求（状态码=0xFE，特殊魔数）
Step 2: 主机进入配对模式（按键长按 5s）
Step 3: 主机扫描到配对请求 → 记录子机 MAC + 分配竿ID
Step 4: 主机广播配对确认（包含竿ID）
Step 5: 子机收到确认 → 存储竿ID到 Preferences（断电保留）
Step 6: 退出配对模式，进入正常运行
```

---

## 5. IMU 鱼口检测算法

### 5.1 当前设计评估

```
幅度 < 0.3g         → 静默/风浪
幅度 0.3-1.2g       → 小鱼闹窝
幅度 1.2-3.5g + 持续 >2s → 中鱼
幅度 >3.5g 瞬时爆发 → 爆口
频率 >20Hz          → 水流/风误报
```

**优点**：
- ✅ 基于幅值和持续时间的多维度判断
- ✅ 频率过滤剔除高频噪音
- ✅ 状态机+SLEEP省电设计合理

**风险**：

**🔴 严重：固定阈值无法适应不同竿型和环境**

| 变量 | 影响 |
|:----|:-----|
| 竿长 (1.8m vs 4.5m) | 同样力度下，长竿梢振幅是短竿的 3-5x |
| 风浪 | 3 级风产生 0.5-1.0g 持续抖动 → 误判为小鱼 |
| 水流 | 急流中竿梢持续低频摆动 |
| 竿硬度 (调性) | 硬竿振幅小，软竿振幅大 |

**建议：自适应阈值算法**

```
不设固定阈值，改用动态基准：

1. 建立静止基准
   - 上电前 1000ms 取均值 → baseX, baseY, baseZ
   - 持续更新：每 60s 在低活动时期更新基准

2. 计算关键特征
   - 动态幅度 = |current - baseline| 的滑动窗口均方根
   - 短时能量 E_short = Σ(amplitude²) over last 0.5s
   - 长时能量 E_long = Σ(amplitude²) over last 5s
   - 峰值频率 = FFT峰值（简化版=过零检测）

3. 自适应阈值
   - 基准噪声 n = RMS(静止期幅度) × 3 (3σ原则)
   - 小鱼阈值 = max(0.2g, n × 2)
   - 中鱼阈值 = 小鱼阈值 × 4
   - 爆口阈值 = 中鱼阈值 × 3（或瞬间变化 > 3倍）

4. 事件持续性判断
   - 连续超过中鱼阈值 > 500ms → 中鱼
   - 瞬间峰值 > 爆口阈值 → 爆口
   - 超过小鱼阈值但 < 中鱼阈值持续 > 2s → 小鱼
```

### 5.2 建议的状态机

```
                  ┌──────────────┐
                  │  CALIBRATE   │ ← 上电 1s 校准
                  │  (自动)      │
                  └──────┬───────┘
                         │ 校准完成
                         ↓
                  ┌──────────────┐
         ┌───────│   SLEEP      │────→ 每 5s 测一次看有无够大的振动
         │ 无振动  │   (低功耗)   │←──  如果静默>60s, 进深睡
         │  > 5s  └──────┬───────┘
         │               │ 检测到振动 (幅度 > 噪声阈值×2)
         │               ↓
         │        ┌──────────────┐
         │        │  SAMPLING    │ ← 启动 100Hz 采样，窗口 2s
         │        │  (分析)      │
         │        └──────┬───────┘
         │               │ 滑动窗口分析
         │               ↓
         │        ┌──────────────┐
         │        │  对比阈值    │
         │        │ (自适应)     │
         │        └──┬───┬───────┘
         │     小鱼/  │   │  中鱼/爆口
         │     误报   │   │
         │           │   ↓
         │           │  ┌──────────────┐
         │           │  │   ALERT      │ ← BLE 广播事件
         │           │  │  (广播)      │
         │           │  └──────┬───────┘
         │           │         │ 广播 3 次后
         │           │         ↓
         │           │     ┌───────────┐
         │           │     │  MONITOR  │ ← 低功耗监听后续
         │           │     │ (监听)    │    如果振动持续，则重广播
         │           │     └─────┬─────┘
         │           │           │ 振动衰减到阈值以下 > 5s
         │           └───────────┘
         └─────────────────────────────────→ SLEEP
```

### 5.3 陀螺仪数据的可选融合

当前设计只用加速度计。如果用陀螺仪数据辅助，可以：

- **区分 平移振动 vs 角度振动**：鱼咬钩通常是竿梢角度变化（陀螺仪敏感），水流是平移振动（加速度计敏感）
- **过滤重力分量**：陀螺仪不受重力影响，更容易提取真实振动

建议算法改进中加入简单的互补滤波：
```
angle = 0.98 × (angle + gyroZ × dt) + 0.02 × accel_angle
```

---

## 6. 功耗与续航

### 6.1 设计声明的续航评估

| 状态 | 声明电流 | CR2032(230mAh) 理论 | 评估 |
|:----|:--------|:-------------------|:-----|
| 深睡 | ~10μA | ~2年 | **🟡 偏高估** |
| 检测中 | ~15mA | ~15小时 | **🟡 偏低估** |
| BLE广播 | ~20mA | ~11小时 | **🟡 偏低估** |

### 6.2 实际功耗估算

**ESP32-C3 实测典型功耗：**

| 状态 | 电流 | 说明 |
|:----|:----|:-----|
| 深睡 (RTC+ULP) | ~5μA | 仅保留RTC内存 |
| 深睡 (MPU6050 连INT) | ~13μA | C3(5μA) + MPU6050 休眠(8μA) |
| 活动 (100Hz采样+分析) | ~18mA | C3(15mA@80MHz) + MPU6050(3.5mA) |
| BLE 广播 (100ms间隔) | ~4.5mA 平均 | 峰值~25mA, 每100ms仅0.6ms, 占空比0.6% |
| BLE 广播 (5s间隔) | ~90μA 平均 | 占空比极低 |

**实际续航估算：**

假设 8 小时出钓：
- 6h 深睡 + 1.5h 偶尔检测 + 0.5h 活跃广播
- 消耗 = 6h×13μA + 1.5h×18mA + 0.5h×(18mA+4.5mA)
- = 78μAh + 27mAh + 11.25mAh = **~38.3mAh**
- CR2032(230mAh) → **可用约 6 次出钓（即 48 小时）**

⚠️ **但 CR2032 的电压问题是更大限制**（见 3.2 隐患2）

### 6.3 功耗优化建议

| 优化项 | 效果 | 实现难度 |
|:-------|:-----|:---------|
| 采样率从 100Hz 降到 50Hz（竿梢振动在15-40Hz为主） | -50% 活动功耗 | 低 |
| 使用 ESP-NOW 替代 BLE 广播（更高效的点对点） | -30% 传输功耗 | 中 |
| 屏熄策略：无鱼口 30s 后熄屏，倾斜唤醒（MPU6886 的 INT） | ~3h→8h+（主机） | 低 |
| 使用 XTAL 16MHz 代替 PLL 160MHz（低速模式） | -40% 活动功耗 | 低 |
| BLE 广播间隔：无鱼时 5s/次 → 10s/次 | -50% 待机广播功耗 | 低 |
| 使用 LiPo 15mAh 微型电池代替 CR2032 | 电压稳定，可充电 | 中 |

---

## 7. 技术风险

### 7.1 风险矩阵

| 风险 | 概率 | 影响 | 等级 | 缓解措施 |
|:----|:----|:----|:-----|:--------|
| CR2032 电压不足驱动 C3 | 高 | 高 | 🔴 | 换 2×AAA 或 14500 LiPo |
| 河边 BLE 距离不足 | 中 | 高 | 🔴 | 实测前不做外壳，保持天线裸露 |
| 风浪误报 | 高 | 中 | 🟡 | 自适应阈值 + 频率过滤 |
| 多竿 BLE 包冲突 | 中 | 中 | 🟡 | 扫描窗口最大化 + 随机延迟广播 |
| 防水不良烧毁 | 高 | 中 | 🟡 | 热缩管 + 704 硅胶密封 |
| 主机续航不足 | 中 | 中 | 🟡 | 自动熄屏 + 按键唤醒 |
| MPU6050 I2C 通信不稳 | 低 | 中 | 🟢 | 添加重试 + 看门狗复位 |
| 邻居鱼竿同步串扰 | 中 | 低 | 🟢 | 魔数过滤 + 配对白名单 |

### 7.2 风险管理计划

**Phase 1 先验证**：
1. CR2032 + ESP32-C3 上电是否稳定（用万用表测）
2. BLE 在空旷河边的实际距离
3. StickC 单竿模式续航

**Phase 2 再解决**：
1. 配对流程设计
2. 自适应阈值算法
3. 多竿共存测试

---

## 8. Firmware 代码结构建议

### 8.1 主机固件 (host-stickc/)

**PlatformIO 项目**：M5StickC Plus (ESP32-PICO-D4)

```
firmware/host-stickc/
├── platformio.ini
├── src/
│   ├── main.cpp                  # 入口，初始化 + 主循环
│   ├── system/
│   │   ├── BleScanner.h          # BLE 扫描器
│   │   ├── BleScanner.cpp
│   │   ├── BleProtocol.h         # 协议定义（或从 shared/ 引用）
│   │   ├── DisplayManager.h      # 屏幕显示管理
│   │   ├── DisplayManager.cpp
│   │   ├── AudioAlert.h          # 蜂鸣器控制
│   │   ├── AudioAlert.cpp
│   │   ├── StateManager.h        # 状态机（多竿/单竿）
│   │   ├── StateManager.cpp
│   │   ├── ImuSingleRod.h        # 单竿模式 IMU
│   │   └── ImuSingleRod.cpp
│   ├── ui/
│   │   ├── ScreenMain.h          # 主屏幕布局
│   │   ├── ScreenMain.cpp
│   │   ├── ScreenDetail.h        # 竿详情
│   │   └── ScreenDetail.cpp
│   ├── storage/
│   │   ├── LogManager.h          # 渔获日志
│   │   ├── LogManager.cpp
│   │   └── ConfigManager.h       # 配置持久化
│   └── utils/
│       ├── Filter.h              # 平滑滤波
│       └── RingBuffer.h          # 环形缓冲区
├── lib/                          # 第三方库（M5StickC 等）
└── test/                         # 单元测试
```

### 8.2 子机固件 (node-esp32c3/)

**PlatformIO 项目**：ESP32-C3

```
firmware/node-esp32c3/
├── platformio.ini
├── src/
│   ├── main.cpp                  # 入口
│   ├── BleBroadcaster.h          # BLE 广播器
│   ├── BleBroadcaster.cpp
│   ├── BleProtocol.h             # 协议定义（或从 shared/ 引用）
│   ├── ImuReader.h               # MPU6050 驱动封装
│   ├── ImuReader.cpp
│   ├── VibrationAnalyzer.h       # 振动分析算法核心
│   ├── VibrationAnalyzer.cpp
│   ├── PowerManager.h            # 睡眠/唤醒管理
│   ├── PowerManager.cpp
│   └── RodConfig.h               # 竿配置（ID, 阈值等）
├── lib/                          # 第三方库
└── test/
```

### 8.3 共享协议定义 (shared/)

```
shared/
└── BleProtocol.h                 # 两边 #include 同一个头文件
```

---

## 9. Phase 1 & 2 骨架代码

Below are the PlatformIO project skeletons and key source files.

### 9.1 Shared BLE Protocol Definition

